repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks

  - repo: https://github.com/semgrep/semgrep
    rev: v1.98.0
    hooks:
      - id: semgrep
        args: ["--config", "auto", "--error"]
