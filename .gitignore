# Environment & Secrets
.env
.env.*
!.env.example
*.pem
*.key
*.crt

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Wrangler local cache
.wrangler/

# AI sandbox — tracked, matching hub/loom/atrade. Contains no secrets:
# config.json holds nulls, notify.py reads TELEGRAM_* from the environment,
# and the OAuth token lives in the `claude-data` Docker volume, never here.

# Lock files
*.~lock.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Project data (generated, not committed)
data/*.db
data/*.db-journal
data/*.json
data/output/
.venv311/
